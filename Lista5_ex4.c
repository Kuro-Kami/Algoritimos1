//4.	Faça um programa em C que receba 10 números e indique quais são maiores que 25 e menores que 85. 
#include <stdio.h>
int main(){
    int a,b,c,d,e,f,g,h,i,j;
    int x=25, y=85;
    printf("Insira 10 valores: \n");
    scanf("%d %d %d %d %d %d %d %d %d %d",&a, &b, &c, &d, &e, &f, &g, &h, &i, &j);
    for(x<y; ;){
    if(a>x && a<y){
        printf("O valor %d e maior que 25 e menor que 85.\n", a);
    }
    if(b>x && b<y){
        printf("O valor %d e maior que 25 e menor que 85.\n", b);
    }
    if(c>x && c<y){
        printf("O valor %d e maior que 25 e menor que 85.\n", c);
    }
    if(d>x && d<y){
        printf("O valor %d e maior que 25 e menor que 85.\n", d);
    }
    if(e>x && e<y){
        printf("O valor %d e maior que 25 e menor que 85.\n", e);
    }
    if(f>x && f<y){
        printf("O valor %d e maior que 25 e menor que 85.\n", f);
    }
    if(g>x && g<y){
        printf("O valor %d e maior que 25 e menor que 85.\n", g);
    }
    if(h>x && h<y){
        printf("O valor %d e maior que 25 e menor que 85.\n", h);
    }
    if(i>x && i<y){
        printf("O valor %d e maior que 25 e menor que 85.\n", i);
    }
    if(j>x && j<y){
        printf("O valor %d e maior que 25 e menor que 85.\n", j);
    }
break;
}
 return 0;
 }  
